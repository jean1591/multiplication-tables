// Polyfill for crypto.getRandomValues() in React Native
// This is needed for the uuid package to work correctly

import "react-native-get-random-values";

// This file doesn't need to export anything, just needs to be imported
// to set up the polyfill globally
